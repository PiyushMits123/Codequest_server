services:
  - type: web
    name: codequest-backend
    env: node
    plan: free
    rootDir: server  # 👈 this is the key part!
    buildCommand: npm install
    startCommand: node index.js  # or whatever your main file is
    envVars:
      - key: PORT
        value: 10000
      -
